# 汉字消消乐 · 使用手册

> 隔一阵没碰忘了怎么用，翻这一页就够。

---

## 〇、这套东西是怎么运转的

```
孩子点开链接 → 玩游戏 → 成绩自动寄到 Google 表格 → 你打开看板扫一眼
```

中间没有一步需要你手工搬运。孩子和家长**不需要注册、不需要下载、不需要点提交**，他们只看到一个游戏页面。

四个组成部分：

| 东西 | 在哪 | 你什么时候碰它 |
|---|---|---|
| 游戏文件 `hanzi.html` | 你电脑上 + GitHub | 加新课时 |
| 网站 | GitHub Pages | 从不（自动的） |
| Google 表单 | 收数据的管道 | 从不（已配好） |
| Google 表格 | 看板 + 学生链接 | 每周看一次 |

---

## 一、常用链接（建议存成书签）

**看板**（每周看）
```
https://docs.google.com/spreadsheets/d/1vzYP6j-1ewROphTbGXaabZ2kISP9FNQpYKLBRmub-08/edit#gid=1876576511
```

**上传游戏文件**（加新课时）
```
https://github.com/sproutlearning-lab/sproutlearning-lab.github.io/upload/main
```

**游戏本身**（自己检查时）
```
https://sproutlearning-lab.github.io/?kids=hebe
```

**表单**（基本不用动）
```
https://docs.google.com/forms/d/15_0qKwciaBFbHNvF1VBfiXSo5M9mcEfTeJrQd8cpjDI/edit
```

**本地文件夹**
```
/Users/owner/Documents/claude/1、海外中文教学/游戏/
```

---

## 二、每周例行

### 备好新课后：加一课（5 分钟）

**方法 A：直接在 GitHub 网页上改**（推荐，不用碰本地文件）

1. 打开 [仓库](https://github.com/sproutlearning-lab/sproutlearning-lab.github.io) → 点 `hanzi.html`
2. 右上角**铅笔图标** → 进入编辑
3. 找到字库（文件最上面），在最后一课的 `},` 后面粘一段新课
4. 拉到底 **Commit changes**
5. 等一两分钟，线上自动更新

**方法 B：改本地文件再上传**

改 `/Users/owner/Documents/claude/1、海外中文教学/游戏/hanzi.html`，然后拖到上传页覆盖。

### 发作业时

**链接照发不误，一个字都不用改。** 不带课号的链接会自动打开最新的一课。

### 周中：看看板（30 秒）

打开看板，扫两栏：
- **本周玩了(次)** —— 谁碰了、谁没碰
- **被错了几次** —— 数字大的字，下节课回炉

---

## 三、加一课，具体怎么写

复制这段，粘到最后一课的 `},` 后面、`];` 前面：

```js
  /* ========== 第 N 课 ========== */
  {
    课名: "种子去旅行 · 第 3 课",

    生字: [
      { 字: "图", 拼音: "tú",  词: "图画" },
      { 字: "画", 拼音: "huà", 词: "画画" },
    ],

    组词: [
      ["图", "书"],
      ["画", "笔"],
    ],
  },
```

- `词` 这一项可以不写，游戏用不到它，写着方便你自己看
- 生字 **8-12 个**最合适；分几轮是自动的，你不用管
- 每一段 `{ }` 之间**必须有逗号**隔开

### 四个最容易踩的坑

| # | 坑 | 对错对照 |
|---|---|---|
| 1 | **中文引号**（最常见） | 错 `{ 字: “种” }`　对 `{ 字: "种" }` |
| 2 | 漏了行尾逗号 | 每行末尾的 `,` 不能少 |
| 3 | 两课之间没逗号 | `},` 那个逗号是用来隔开两课的 |
| 4 | 拼音写成数字 | 错 `zhong3`　对 `zhǒng` |

**改坏了不会白屏**：会出现一个 🔧 页面，直接告诉你哪里不对、按什么顺序检查。照着改就行。

---

## 四、组词怎么配（重要）

### 两个模式用的是两份清单

| 模式 | 用哪份 | 规则 |
|---|---|---|
| 汉字配拼音 | `生字` | **只放本课生字** |
| 两字组词 | `组词` | 本课生字 **配一个课外的简单字** |

### 三条规则

**1. 配课外的字，而且要更简单**

一是顺手复习或拓展别的字，二是孩子有个认识的字当抓手。配的字太难，两个都不认识，孩子会有挫败感。

优先挑：启蒙 50 字（水、看、不、子、有、来、去）· 以前课教过的（查 `参考资料/生字总表.md`）· 日常生活字（书、球、米、学、门）。

**2. ⚠️ 每组只放一个本课生字**

```
对：["书","包"]   书是课外字 → 这组算在「包」头上
错：["果","皮"]   果和皮都是本课生字 → 会算在「果」头上，「皮」就永远统计不到
```

**3. 实在避不开，就指定要记哪个字**

有些词两个字都是本课生字（身体、海洋），这时在后面**再写一个字**：

```js
["身", "体", "体"]    // 意思：这组算在「体」头上，不是「身」
["海", "洋", "洋"]
```

### 同一个字可以重复用

「看」在观看、察看里各用一次完全没问题 —— 游戏按**字面**判对错，孩子点任意一张「看」都算对。高频字多出现几次反而是好事。

### 生字在前在后都行

`米粒`（生字在后）、`皮球`（生字在前）都可以，按词本来的顺序写。游戏会自己认出哪个是本课生字。

### ⚠️ 多音字

**配不出本课那个读音的常用词，就不给它配组词**，少一组比教错音强。

已经这么处理的：
- **数**（本课读 shǔ，但数学/数字全是 shù）
- **浮**（只在「漂浮」里出现，那组算在「漂」头上）

---

## 五、复习合集

合集是特殊的一课，用来滚动复习旧字。跟普通课有三点不同：

```js
{
  课名: "一二学期合集（上）",
  复习合集: true,      // ← 不带课号的链接不会打开它，永远打开最新正课
  每次随机抽: 12,      // ← 89 个字不会一次全出，每次随机挑 12 个，每次不重样

  生字: [ ... ],
  组词: [ ... ],       // 留空 [] 的话，「两字组词」按钮会自动隐藏
}
```

**为什么要随机抽**：一两百个字全摆出来要玩几十轮，孩子撑不到一半。随机抽之后每次都是新面孔，正好适合反复复习。

**孩子怎么进合集**：首页课名条上点橙色的「🔄 换一课」。

现在有两个合集：一二学期合集（上）89 字、（下）82 字，都配好了组词。

---

## 六、发给学生

### 链接格式

| 情况 | 链接 |
|---|---|
| 一个孩子 | `https://sproutlearning-lab.github.io/?kids=alvin` |
| 一家两个 | `?kids=hebe,freya` |
| 一家三个 | `?kids=amy,leo,mia` |
| 指定玩第 2 课 | `?kids=alvin&lesson=2` |

**不写课号 = 自动打开最新一课**，所以链接发一次就够，加新课不用重发。

### 代号规矩

- **只用英文小写字母**，同名学生加姓氏首字母（`amy-l` / `amy-w`）
- 不能有中文、大写、空格 —— 会让链接失效
- 学生链接表的最后一列会自动提醒你

### 一家多个孩子

一条链接全家共用。打开先出现「今天是谁在玩？」，每个名字一个固定颜色。选完进游戏，**各人的记录分开存，互不覆盖**。

### 新学生入班

1. Google 表格「学生链接」页加一行 → 链接自动生成
2. Google 表格「看板」页 A 列加代号 → 选中上一行的 B、C、D 三格往下拖

---

## 七、看板怎么看

| 列 | 意思 |
|---|---|
| 本周玩了(次) | 0 就是这周没碰 |
| 平均正确率 | 只算本周的 |
| 最近一次玩 | 显示 `—` 表示从来没玩过；显示上周日期 = 以前玩过、这周断了 |
| **被错了几次** | **最有价值的一栏**，数字大的字下节课回炉 |

「本周」从周一算起，每周一自动重置。

---

## 八、出问题了怎么查

| 症状 | 原因 | 怎么办 |
|---|---|---|
| 打开是 🔧 页面 | 字库写错了 | 照页面上的提示逐条检查；实在不行用备份覆盖 |
| 首页写「这台设备不记录游戏次数」 | 双击本地文件打开的（`file://`） | 正常现象，用线上链接打开就好 |
| 表格里没有某个孩子的数据 | 代号拼写对不上（`Boniu` vs `boneo`） | 看板 A 列和链接里的代号必须**完全一致** |
| 改完线上没变 | GitHub 部署要一两分钟 | 等一下，然后按 `Cmd + Shift + R` 硬刷新 |
| 孩子说玩了但表里没有 | 当时没网 | 正常，下次他打开游戏会自动补寄 |

**动字库前先备份**：把 `hanzi.html` 复制一份存着，改坏了直接覆盖回去，比排查快得多。

---

## 九、几条不要改的约定

1. **生字必须是楷体** —— 笔画形态接近手写，孩子照着认、照着写都对得上
2. **中文必须是真文字，不能是图片**
3. **仓库是公开的** —— 别往里放学生真名单、家长联系方式、成绩记录
4. **学生链接管理表留在 Google 表格里**，别放进「游戏」文件夹
