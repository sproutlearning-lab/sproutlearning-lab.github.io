# 消消乐 · 字库改法

每周加新课就看这一页。要改的只有一个文件：`游戏/hanzi.html`。

> **动手前先复制一份备份**（把 `hanzi.html` 复制成 `hanzi-备份.html`）。
> 改坏了直接把备份改回来，比排查错误快得多。

---

## 一、加一课，三步

### 第 1 步：打开文件，找到字库

用任何文本编辑器打开 `hanzi.html`（VS Code、Sublime、甚至「文本编辑」都行）。

字库在**文件最上面**，长这样，很好认：

```
const 全部课程 = [

  /* ========== 第 1 课 ========== */
  {
    课名: "种子去旅行 · 第 1 课",
    ...
  },

];
```

### 第 2 步：在最后一课后面，粘贴一段新课

把下面这段**整个复制**，粘贴到最后一个 `},` 的后面、`];` 的前面：

```js
  /* ========== 第 2 课 ========== */
  {
    课名: "种子去旅行 · 第 2 课",

    生字: [
      { 字: "根", 拼音: "gēn", 词: "树根" },
      { 字: "芽", 拼音: "yá",  词: "发芽" },
    ],

    组词: [
      ["树", "根"],
      ["发", "芽"],
    ],
  },
```

### 第 3 步：把内容换成你的字

- `课名` —— 显示在游戏首页，孩子一眼知道玩的是哪一课
- `生字` —— 一行一个字，几个字就写几行
- `组词` —— 一行一组，两个字连成一个词

保存，刷新浏览器，完事。

---

## 二、可以直接复制的空模板

```js
  /* ========== 第 N 课 ========== */
  {
    课名: "",

    生字: [
      { 字: "", 拼音: "", 词: "" },
      { 字: "", 拼音: "", 词: "" },
      { 字: "", 拼音: "", 词: "" },
    ],

    组词: [
      ["", ""],
      ["", ""],
    ],
  },
```

---

## 三、四个最容易踩的坑

| # | 坑 | 长什么样 |
|---|---|---|
| 1 | **中文引号**（最常见） | 错：`{ 字: “种” }` 　对：`{ 字: "种" }` |
| 2 | **漏了逗号** | 每行末尾的 `,` 不能少 |
| 3 | **两段课之间没逗号** | `},` 的那个逗号是用来隔开两课的，不能删 |
| 4 | **拼音写成数字** | 错：`zhong3`　对：`zhǒng`（声调直接打出来） |

**改坏了会怎样**：游戏不会白屏，会出现一个 🔧 页面，直接告诉你哪里不对、按什么顺序检查。看提示改就行。

拼音声调不好打的话，在线搜「汉语拼音声调输入」，或者直接从你的教案 / 生字总表里复制。

---

## 四、给量的建议

| 项目 | 建议 | 为什么 |
|---|---|---|
| 生字 | **8-12 个** | 少于 6 个太快玩完；多于 12 个手机上排不下 |
| 组词 | **5-9 组** | 太少的话组词模式几十秒就结束了 |

**分轮是自动的**，你不用管：游戏会把生字尽量平均分成几轮，每轮 5 对左右。

| 你放几个字 | 自动分成 |
|---|---|
| 6 个 | 3 + 3 |
| 9 个 | 5 + 4 |
| 10 个 | 5 + 5 |
| 12 个 | 4 + 4 + 4 |

---

## 五、两个模式的配字规则（重要）

| 模式 | 用哪份清单 | 规则 |
|---|---|---|
| 汉字配拼音 | `生字` | **只放本节课的生字**，不掺别的 |
| 两字组词 | `组词` | 本课生字 **配一个课外的简单字** |

### 组词为什么要配课外的字

一是顺手复习或拓展别的字词，二是孩子有个认识的字当抓手。

**配的那个字一定要比生字简单**，否则两个都不认识，孩子会有挫败感。优先挑这几类：

- 启蒙 50 字里的（水、看、不、子、有、来、去……）
- 以前课教过的（查 `参考资料/生字总表.md`）
- 日常生活里天天见的（书、球、米、学、门）

第 1 课的实际配法：

| 生字 | 组词 | 配的是 |
|---|---|---|
| 种 | 种**子** | 启蒙字 |
| 粒 | **米**粒 | 日常字 |
| 果 | **水**果 | 启蒙核心字 |
| 皮 | 皮**球** | 日常字 |
| 包 | **书**包 | 日常字 |
| 藏 | **躲**藏 | 日常字 |
| 观 | 观**看** | 启蒙核心字 |
| 察 | 察**看** | 启蒙核心字 |
| 同 | 同**学** | 日常字 |

### 两条会踩到的细节

**1. 同一个字可以在多组里重复用**（上面「看」用了两次）。游戏按**字面**判对错，不按是哪张牌，所以孩子点任意一张「看」都算对。放心重复，高频字多出现几次反而是好事。

**2. 生字在词里是前是后都行**（米**粒** 生字在后，**皮**球 生字在前）。按词本来的顺序写就好，游戏会自动认出哪个是本课生字 —— 记错字时记的是生字（「包」），不是整个词（「书包」）。

### ⚠️ 多音字要当心

「数」本课读 **shǔ**（数一数），但常见的双字词「数学」「数字」全读 **shù**。配了就等于教错音，所以第 1 课**没给「数」配组词**，它只出现在「汉字配拼音」里。

以后遇到多音字，同样的判断：**配不出本课那个读音的常用词，就不配**，少一组比教错音强。

---

## 六、链接怎么写

| 想要的效果 | 链接末尾写什么 |
|---|---|
| 一个孩子，玩最新一课 | `?kids=amy` |
| 一家两个孩子 | `?kids=amy,leo` |
| 指定玩第 2 课 | `?kids=amy&课=2` |

**不写课号 = 自动打开最新的一课。**

这条很重要：意味着**你每周加完新课，不用重新发链接** —— 家长手里的老链接会自己指向新课。课号只在你想让孩子复习旧课时才用得上。

字库里有第 2 课以后，游戏首页会自动出现「换一课」按钮，孩子可以自己回头玩旧课。

---

## 七、改完怎么验证

1. 保存文件，浏览器**刷新**（Mac 上是 `Cmd + R`）
2. 首页课名对不对
3. 首页写的「共 N 个字」对不对
4. 进「汉字配拼音」，玩完整轮，看生字有没有全出现
5. 进「两字组词」，看配对对不对
6. 有第 2 课的话，点「换一课」，看两课都在列表里

如果出现 🔧 页面，按上面的提示改；实在搞不定就用备份文件覆盖回去。
